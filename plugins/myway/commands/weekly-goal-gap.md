---
description: Measure the gap between weekly goals and GitLab evidence
argument-hint: "[--days N | --since DATE [--until DATE]] [milestone]"
---

Run the gap arc of the `weekly-goal` skill. Read and follow, end to end:

`${CLAUDE_PLUGIN_ROOT}/skills/weekly-goal/references/gap.md`

The fetch script is `${CLAUDE_PLUGIN_ROOT}/skills/weekly-goal/scripts/gitlab_activity.py`; configuration and output shapes are in `${CLAUDE_PLUGIN_ROOT}/skills/weekly-goal/references/config.md`.

Arguments: $ARGUMENTS
